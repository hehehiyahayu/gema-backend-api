class statusesModel {
    constructor(status_id, status_name, status_type){
        this.status_id = status_id
        this.status_name = status_name
        this.status_type = status_type
    }
}

module.exports = statusesModel